CheckMind backend on Flask

Setup:
```
git clone <ссылка_на_репозиторий>
cd CheckMindFlask/
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
deactivate
```
Start:
```
python3 main.py
```
